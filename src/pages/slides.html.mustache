<!doctype html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>porto.codes</title>
    <link rel="stylesheet" type="text/css" href="main.css"/>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body class="projection">
    <header role="banner" tabindex="1">
      <a href="#"><span>Porto</span> <span>Codes</span></a>
    </header>

    <main>
      {{#current.sponsors}}
      <section class="sponsors" id="sponsor-{{.}}" tabindex="1">
        <img src="/sponsors/{{.}}.png" />
      </section>
      {{/current.sponsors}}

      <section id="introduction" tabindex="2">
        <p><span class='h2'>Porto Codes</span>  <a href="">https://porto.codes</a></p>
        <p>Meet-up on the second Thursday of each month</p>
        </p>

        <p><span class='h2'>Conversas em Código</span> <a href="https://conversasemcodigo.pt">https://conversasemcodigo.pt</a></p>
        <p>Podcast in portuguese about programming</p>
      </section>

      <section id="schedule" tabindex="3">
        {{#current}}
        <h1>Schedule</h1>

        <ol>
          {{#schedule}}
          {{#talk?}}
          <li>
            <time class="dt-start" datetime="{{date}} {{start}}">{{start}}</time>
            <p>{{title}}{{#speaker}}, by {{name}}{{/speaker}}</p>
          </li>
          {{/talk?}}
          {{/schedule}}
        </ol>
        {{/current}}
      </section>

      <section id="upcoming" tabindex="4">
        <h1>Upcoming events</h1>

        {{#editions}}
          <h2>{{title}}</h2>
          <p><b>{{human_date}}</b></p>

          {{#schedule}}
          {{#talk?}}
          <p>{{title}}{{#speaker}}, by <a href="{{speaker.url}}">{{speaker.name}}</a>{{/speaker}}</p>
          {{/talk?}}
          {{/schedule}}

        {{/editions}}
      </section>

      <section id="speak" tabindex="5">
        <h1>Speak at Porto Codes</h1>

        <p>
          We're always looking for someone to speak at Porto Codes. We're
          pretty flexible when it comes to the topic, and we welcome all levels
          of expertise.
        </p>

        <p>Lightning talks are welcome.</p>

        <p>If you do speak at Porto Codes, dinner and ice cream is on us!</p>


        <p class="cta"><a href="">https://porto.codes</a></p>
      </section>

      <section id="other" tabindex="6">
        <h1>Other events this month</h1>

        {{#current.other}}
        <p>{{date}}: {{title}}</p>
        <p class="event-url"><a href="{{url}}">{{url}}</a></p>
        {{/current.other}}
      </section>
    </main>
  </body>
</html>
